<?xml version="1.0" encoding="utf-8"?>
<ContentDialog x:Class="MyMediaPlayer.Views.CreatePlaylistDialog"
               xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
               xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
               xmlns:local="using:MyMediaPlayer.Views"
               xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
               xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
               mc:Ignorable="d"
               Background="{ThemeResource ApplicationPageBackgroundThemeBrush}"
               CornerRadius="16"
               Title="Playlist's name:" HorizontalAlignment="Center">

    <StackPanel>
        <TextBox x:Name="playlistNameTextBox" />
        <Button Content="Create playlist"
                HorizontalAlignment="Center"
                Margin="10"
                Background="{ThemeResource ApplicationPageBackgroundThemeBrush}" />
    </StackPanel>
</ContentDialog>
